import React from "react";

const NavBar = () => {
    return (
        <header>
            <nav className="Nav-bar">
                <h1>Fråga Flashback</h1>
            </nav>
        </header>
    );
};

export default NavBar;
